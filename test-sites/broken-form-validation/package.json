{
  "name": "broken-form-validation-test",
  "version": "1.0.0",
  "type": "module",
  "description": "Test site with intentional form validation bugs for debug-agent testing",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node server.js",
    "start:prod": "NODE_ENV=production node server.js",
    "serve": "npm run build && npm run start:prod",
    "test": "echo 'Run debug-agent to find the bugs!'"
  },
  "private": true,
  "devDependencies": {
    "terser": "^5.43.1",
    "vite": "^7.1.2"
  }
}
